window.addEventListener('error', (e) => { fetch('http://localhost:3000/log-error', { method: 'POST', body: JSON.stringify({ message: e.message, filename: e.filename, lineno: e.lineno, colno: e.colno, error: e.error?.stack }) }) }); window.addEventListener('unhandledrejection', (e) => { fetch('http://localhost:3000/log-error', { method: 'POST', body: JSON.stringify({ message: e.reason?.message, stack: e.reason?.stack }) }) });
